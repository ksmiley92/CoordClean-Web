# Copy to backend/.env and fill in values. Never commit .env.

# Local dev (Stripe test mode)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PRICE_ID=price_...
FRONTEND_BASE_URL=http://127.0.0.1:5500/frontend
FREE_ROW_LIMIT=10

# Production only — your Hostinger frontend origin (https, no trailing slash)
# CORS_ORIGIN=https://YOUR_DOMAIN
